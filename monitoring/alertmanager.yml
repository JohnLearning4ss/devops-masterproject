global:
  smtp_smarthost: "smtp.gmail.com:587"
  smtp_from: "ajohnpeter00@gmail.com"
  smtp_auth_username: "ajohnpeter00@gmail.com"
  smtp_auth_password: "slngzsavemjssmta"  # replace this
  smtp_require_tls: true

route:
  receiver: "email-alert"
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: "email-alert"
    email_configs:
      - to: "ajohnpeter00@gmail.com"
        send_resolved: true
        headers:
          Subject: "🚨 DevOps Alert: {{ .GroupLabels.alertname }}"
